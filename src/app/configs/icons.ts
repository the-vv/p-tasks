import { addIcons } from "ionicons";
import {
    add,
    arrowForward,
    timeOutline,
    closeOutline
} from 'ionicons/icons';

export const setupIcons = () => {
    addIcons({
        add, arrowForward, timeOutline, closeOutline
    })
}