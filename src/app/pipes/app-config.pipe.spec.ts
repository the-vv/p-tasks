import { AppConfigPipe } from './app-config.pipe';

describe('AppConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new AppConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
